import { Theme } from 'components/config/theme';
import { AddressFormProps, StyleKeys } from './types';

export default (
  theme: Theme
): StyleFnReturnType<StyleKeys, AddressFormProps> => ({
  component: {},
});
