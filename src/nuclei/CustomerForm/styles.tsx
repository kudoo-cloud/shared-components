import { Theme } from 'components/config/theme';
import { CustomerFormProps, StyleKeys } from './types';

export default (
  theme: Theme
): StyleFnReturnType<StyleKeys, CustomerFormProps> => ({
  component: {},
});
